# Solace Agent Mesh Orchestrator Agent Configurations

log:
  stdout_log_level: INFO
  log_file_level: INFO
  log_file: glass-box-orchestrator.log

# Shared SAM config
!include ../shared_config.yaml

apps:
  - name: glass-box-orchestrator_app
    app_base_path: .
    app_module: solace_agent_mesh.agent.sac.app
    broker:
      <<: *broker_connection

    app_config:
      namespace: ${NAMESPACE} 
      supports_streaming: true
      agent_name: "GlassBoxOrchestrator"
      display_name: "GlassBoxOrchestrator"
      model: *planning_model 

      instruction: |
        You are the Orchestrator Agent within the Glass Box system. Your primary responsibilities are to:
        1. Process tasks received via the GlassBoxGateway.
        2. Analyze each task to determine the optimal execution strategy:
           a. Single Agent Delegation: If the task can be fully addressed by a single peer agent, delegate the task to that agent.
           b. Multi-Agent Coordination: If task completion requires multiple agents, create a concise plan and coordinate execution in order.
           c. Direct Execution: If the task does not require delegation, execute it yourself.

        Glass Box workflow rules:
        - Humans create nodes and define inputs before execution. Do not create top-level nodes.
        - Subnodes are allowed only when necessary and within the provided budget.
        - Agent-assigned tasks must include evidence before completion.
        - Inputs may include node references; treat referenced node outputs/evidence as context.
        - Use artifact tools to produce files, then request the gateway to attach evidence/outputs.

        Artifact Management Guidelines:
        - Review your artifacts and return the ones that matter using artifact_return.
        - Provide progress updates using status_update, especially before tool calls or approvals.
      inject_system_purpose: true
      inject_response_format: true
      inject_user_profile: true
      session_service: 
        type: "sql"
        database_url: "${ORCHESTRATOR_DATABASE_URL, sqlite:///orchestrator.db}"
        default_behavior: "PERSISTENT"
      artifact_service: *default_artifact_service
      artifact_handling_mode: "reference" 
      enable_embed_resolution: true 
      enable_artifact_content_instruction: true 
      data_tools_config: *default_data_tools_config
      tools: 
        - group_name: artifact_management
          tool_type: builtin-group
        - tool_type: builtin-group
          group_name: "data_analysis"

      agent_card:
        description: "The Orchestrator component. It manages tasks and coordinates multi-agent workflows."
        defaultInputModes: [text]
        defaultOutputModes: [text, file]
        skills:
        - id: strategic_planning
          name: Strategic Planning
          description: Analyzes complex requests and creates structured execution plans, breaking down multi-step goals into logical sequences with clear checkpoints and progress tracking.

        - id: agent_coordination
          name: Agent Coordination
          description: Identifies the most suitable specialized agents for specific tasks and coordinates multi-agent workflows, managing data handoffs and execution sequences to achieve complex objectives efficiently.

        - id: artifact_management
          name: Artifact Management
          description: Creates, transforms, and manages various artifact types including documents, reports, and data files, with capabilities for content generation, formatting, embedding dynamic data, and applying sophisticated data transformations.

      agent_card_publishing: 
        interval_seconds: 10
      agent_discovery: 
        enabled: true
      inter_agent_communication:
        allow_list: ["*"]

        request_timeout_seconds: 600
